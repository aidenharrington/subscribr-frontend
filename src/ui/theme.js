import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
        main: '#4997D0', // Baby blue
        },
        secondary: {
        main: '#333333', // Grey
        },
    }
});

export default theme;